#!/usr/bin/env bash
set -euo pipefail && cd "$(dirname "${BASH_SOURCE[0]}")/.."

exec clojure \
     -A:nrepl:rebel \
     -i dev/init.clj \
     -e "(do (require 'advent.puzzle06) (in-ns 'advent.puzzle06) nil)" \
     -m rebel-readline.main \
     "$@"
